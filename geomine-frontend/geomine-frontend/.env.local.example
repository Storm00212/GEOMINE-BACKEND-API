# Supabase project settings — SAME project as the backend uses.
# The frontend only ever uses the anon key, for its own login/session
# management. It never touches the service role key — that lives on the
# backend only.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Where the separate backend deployable is running.
# Local dev default: http://localhost:4000
# Production: your deployed backend's URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
