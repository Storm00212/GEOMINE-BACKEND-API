import { correctReadingController, deleteReadingController } from "@/lib/modules/readings/readings.controller";

export const PATCH = correctReadingController;
export const DELETE = deleteReadingController;
